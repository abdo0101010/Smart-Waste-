namespace SmartWaste.Enums
{
    public enum TicketStatus
    {
        Open = 0,
        InProgress = 1,
        Resolved = 2
    }

    public enum TicketPriority
    {
        Low = 0,
        Medium = 1,
        High = 2,
        Critical = 3
    }
}
